/* TODO: I don't know what effect the 'reverse.h' has on the reverse fold */

#pragma once  # To assure this header file will be compiled only once.

void reverse( int* A, int lo, int hi );

void reverse( int* A, int n );